# add-odcli-environment-lifecycle

Add odcli project init and development environment lifecycle (Git worktree, generated odoo.conf, Python env binding, durable catalog v3, run/shell coordinators, doctor) per issue #3
