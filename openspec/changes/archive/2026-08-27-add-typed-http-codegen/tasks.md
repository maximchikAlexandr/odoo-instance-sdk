## 1. Baseline and typed contracts

- [x] 1.1 Update local `main`, merge/rebase completed MYL-55 into `feat/MYL-58-typed-http-codegen`, and verify its snapshot v2, required `observed_port`/`artifacts`, removed-row semantics, CLI envelope v1, and canonical public-method characterization are present before making any MYL-58 edit; resolve only shared plan-listed snapshot/CLI test changes and confirm no unrelated work entered the branch.
- [x] 1.2 Add frozen unknown-field-forbidden `PgAdminEligibility`, `PgAdminOpenRequest`, `PgAdminOpenResult`, and `HttpError` models plus exact enums, including `HttpErrorCode.invalid_request`, to public exports; unit-test construction, enum values, unknown-field rejection, and redacted repr behavior.
- [x] 1.3 Add only required `EnvironmentSnapshot.pgadmin` to the MYL-55 v2 shape, bump snapshot schema version from 2 to 3, preserve required `observed_port`/`artifacts` and all MYL-55 collection/filter/removed-row/CLI semantics, update shared Python/CLI/HTTP fixtures, and unit-test v3 plus eligibility precedence without database or Docker mutation.
- [x] 1.4 Update exactly `tests/unit/test_cli_characterization.py::test_discovered_public_methods` so the MYL-55 canonical table gains `EnvironmentResource.open_pgadmin`; assert the remainder of the discovered public method set is byte-for-byte/equality-identical to the MYL-55 baseline.

## 2. Modular typed HTTP adapter

- [x] 2.1 Move app/server composition, loopback Host policy, port selection, health/static mount, browser boot, and lazy optional imports into `odoo_instance_sdk.http.app`; move the monitor router/lock/serialization into `odoo_instance_sdk.http.monitor`; leave `internal.serve` as a thin compatibility re-export and prove existing boot behavior with focused tests.
- [x] 2.2 Implement the single msgspec JSON Schema to OpenAPI component/reference adapter for the exact route model set, with stable unique component names and no Pydantic DTO copy; unit-test nested nullability/enums and that every emitted `$ref` resolves.
- [x] 2.3 Publish `GET /api/v1/snapshot` as `getMonitorSnapshot` with direct `Snapshot` bytes and named `HttpError` 500 response; test project filtering, content type, absence of CLI envelope fields, operation ID, component references, and sanitized failure payload.
- [x] 2.4 Add production-byte contract tests that resolve the real `create_app(...).openapi()` response schemas and validate representative snapshot and HTTP error JSON values against them.
- [x] 2.5 Register the pgAdmin router only for UI composition; add the private `static_assets=True` composition keyword and test that normal UI runtime still requires/mounts packaged `web/dist`, while `headless=False, static_assets=False` with injected schema monitor/opener documents the route in a clean checkout with missing `dist` and performs no resource, source-tree, Docker, credential, socket, or browser side effect.

## 3. Public pgAdmin application operation

- [x] 3.1 Add dedicated pgAdmin domain exceptions and `EnvironmentResource.open_pgadmin(selector)` resolution: load the catalog environment/generated config, recompute eligibility, construct the existing configured instance, and map missing/ineligible/inconclusive states without accepting browser endpoint data.
- [x] 3.2 Reuse `DatabaseResource.exists()` before any pgAdmin file/Docker operation and unit-test present, confirmed-missing, and inconclusive checks, including assertions that rejected calls perform no lifecycle mutation.
- [x] 3.3 Add private user-level pgAdmin paths, a private per-user random HMAC key, and a single global lock using existing path/lock primitives; on Linux implement the exact no-sudo invoking-user-owned ACL layout (`0710` traversal parents, `0640` secret/config files, owner-only `0600` HMAC key, `0770` persistent data, empty owning-group/other entries, only named UID 5050 access plus required data default ACL), and test containment, exact entries/masks/modes/no extra grants, HMAC rotation/non-derivability, concurrent first calls, and fail-closed missing-ACL/symlink/type/ownership cases.
- [x] 3.4 Implement atomic creation/reuse of the administrator password, PostgreSQL passfile, server JSON, HMAC-based non-secret fingerprint/port metadata, and persistent data directory; mount secrets/config read-only and `/var/lib/pgadmin` read-write, verify UID 5050 read/write behavior without sudo/chown or owning-group/other effective access, and test that existing secrets are never overwritten or exposed.
- [x] 3.5 Implement the minimal Docker CLI lifecycle in private container/reconciliation and readiness modules, with only `open_pgadmin_lifecycle` orchestrating them, using a reviewed official image digest and exact startup contract: `PGADMIN_CONFIG_SERVER_MODE=False`, `PGADMIN_REPLACE_SERVERS_ON_STARTUP=True`, `PGADMIN_DEFAULT_EMAIL=odoo-instance-sdk@localhost.invalid`, `PGADMIN_DEFAULT_PASSWORD_FILE=/run/odoo-instance-sdk/pgadmin-admin-password`, and `PGPASS_FILE=/run/odoo-instance-sdk/pgpass`; retain SDK labels, loopback-only publish, no socket mount, and bounded readiness, and assert no secret literals in argv/environment metadata/labels/logs.
- [x] 3.6 Resolve the selected SDK Compose network/container identity, connect only the shared pgAdmin container to the required network, and generate one official server definition with exact `MaintenanceDB`/`DBRestriction`; test fresh import, matching reuse without recreate, and cross-project recreate with replace-on-start plus effective changed database before `reconfigured`, and verify the root has no login prompt, undocumented deep link, or second SDK catalog.
- [x] 3.7 Add `POST /api/v1/pgadmin/open` as `openPgAdmin`, strictly decode raw bytes into the unknown-field-forbidden request with msgspec, and delegate only to `EnvironmentResource.open_pgadmin`; parameterize malformed JSON, missing/non-string `environment_id`, and unknown fields to assert exact `422 HttpError(code="invalid_request", message="invalid request")`, schema validation, no leaked decoder/input detail, and opener-not-called, alongside exact 200/404/409/503 contracts.

## 4. Deterministic OpenAPI and TypeScript generation

- [x] 4.1 Add a side-effect-free export script that calls `create_app(headless=False, static_assets=False, monitor=<schema stub>, pgadmin_opener=<typed no-op>).openapi()`, rejects forbidden machine-local/secret values, and atomically writes sorted canonical root `openapi.json`; test byte identity across two runs and success with missing `web/dist`, while spies forbid source-tree asset access, resource construction, Docker, credentials/config, sockets, server, and browser actions.
- [x] 4.2 Exact-pin `@hey-api/openapi-ts` in frontend package/lock files and configure only generated models, Fetch client, flat operations, and `@generated` headers under `web/src/generated/` without Axios, hooks, mocks, or custom templates.
- [x] 4.3 Run and record the Hey API adoption gate: assert callable `getMonitorSnapshot`/`openPgAdmin`, exact nullability/enums, flat tree-shakeable imports, generated headers, TypeScript compile, forbidden-output scan, and byte-identical double generation; if any assertion fails, replace it only with exact-pinned `openapi-typescript` + `openapi-fetch` and rerun the same gate.
- [x] 4.4 Implement `make web-codegen` and a non-destructive temporary-output `make web-codegen-check` that compares two generated runs with each other and committed OpenAPI/generated files; add focused tests that stale and nondeterministic output fail without rewriting the developer worktree.

## 5. React generated-client migration

- [x] 5.1 Replace handwritten `web/src/api.ts` models/wrappers with one small handwritten relative-base-URL client configuration module and update all imports/tests to generated models and callable Fetch operations; add a source assertion preventing direct endpoint-specific `fetch()` and duplicate API interfaces.
- [x] 5.2 Preserve serialized two-second snapshot polling through `getMonitorSnapshot`, including current loading/retry behavior, and update frontend tests to mock the generated client rather than global endpoint fetch wrappers.
- [x] 5.3 Add the environment-card pgAdmin state/explanation and pending-safe button using generated `PgAdminEligibility`; on eligible success call generated `openPgAdmin` and open only its returned loopback root URL with `noopener,noreferrer`; test disabled states, duplicate-click prevention, typed errors, and successful browser open.

## 6. Verification and delivery

- [x] 6.1 Add/extend backend tests for core-only imports, CLI/`internal.serve` compatibility, headless security boundary, schema uniqueness, exact invalid-request redaction/non-delegation, additive snapshot v2→v3 compatibility, pgAdmin single-user bootstrap/UID 5050 ACLs/mounts/locking/security/networking, private HMAC identity and module boundaries, and a disposable Docker smoke path proving login-free fresh, reused, cross-project reconfigured, and same-backend password-rotated behavior for one shared container.
- [x] 6.2 Define the clean-checkout dashboard CI order exactly: install Python dashboard/test dependencies, run frontend `npm ci`, run `make web-codegen-check` while `web/dist` is absent, run frontend tests, then `npm run build`; ensure backend contract/package checks do not create `dist` as a codegen prerequisite and core-only installs remain dashboard-dependency-neutral.
- [x] 6.3 Run `make web-codegen-check`, frontend tests/build, targeted dashboard/pgAdmin/contract tests, package tests, full backend tests, lint, and strict types; regenerate only through the canonical command until all local checks pass.
- [x] 6.4 Verify the selected remote push URL is `git@github.com:maximchikAlexandr/odoo-instance-sdk.git` or equivalent SSH, confirm the final verified commit is pushed to `feat/MYL-58-typed-http-codegen`, and deliver the branch without a PR per the approved delivery override.
