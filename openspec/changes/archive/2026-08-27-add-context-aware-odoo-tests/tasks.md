## 1. Foundation and Characterization

- [x] 1.1 Rebase or merge the accepted MYL-55 implementation onto `feat/MYL-56-changed-odoo-tests`; verify `commands/context.py`, `commands/output.py`, command-local `--format rich|json|toon`, `--json`, and envelope v1 exist, and resolve this change against that implemented surface without recreating foundation helpers.
- [x] 1.2 Add characterization tests for current `odcli module test`: required plural modules and `--test-tags`, runner arguments, free-port failure, zero/failure/success counts and exits, `--allow-empty`, JSON envelope, sanitized stderr, and environment resolution; prove no behavior changes before refactoring.
- [x] 1.3 Add/adjust root and module help snapshots so the pre-change tree is pinned and the later additive top-level `test` entry and options are the only intended command-surface differences.

## 2. Typed Contract and Safe Selection

- [x] 2.1 Add frozen public `OdooTestSpec` and `OdooTestResult` models to the existing model/export surface, including the exact five count keys and operation-boundary validation for non-empty sorted unique modules and native tags; add serialization/import/validation unit tests and no public `TestResource`.
- [x] 2.2 Create Click-free `internal/test_selection.py` with private typed plan/provenance values and eligible-root normalization from the bound worktree plus generated `StartConfig.addons_path`; test relative roots, missing/external roots, canonical containment, duplicate module names, traversal, symlinked roots/modules/manifests, and deterministic candidate diagnostics.
- [x] 2.3 Implement bare-module, nearest-cwd-manifest, and explicit `test_*.py` resolution with default native selectors `/<module>` and `/<module>/tests/<path>`; test project-root/no-addon errors, nested cwd, absolute/relative files, `__init__.py`, non-Python/missing files, outside-`tests` paths, file symlinks, and exact `--tags` passthrough policy.
- [x] 2.4 Implement the bounded byte-oriented Git runner and base resolution (`--base`, then recorded non-`HEAD` environment base); verify base/HEAD/merge-base, reject missing/`HEAD` fallback, use argv plus `shell=False`, NUL parsing, time/output bounds, no remote commands, and fail if HEAD changes during collection.
- [x] 2.5 Implement the four-state changed-path union using committed, staged, unstaged, and untracked non-ignored queries with rename detection disabled; normalize/sort/deduplicate filenames and map them into sorted direct modules, `ignored_paths`, and fatal `unmapped_paths` without computing dependants; for executable changed selection without `--tags`, build the exact comma/no-whitespace selector from sorted unique `/<module>` tokens, while assigning explicit `--tags` byte-for-byte.
- [x] 2.6 Add temporary-Git-repository tests covering all four states together, overlapping paths, add/delete/rename across addons, docs-only/no-addon success, `.gitignore`, spaces/newlines/shell metacharacters and filesystem-decoded names, missing manifests, nested nearest manifests, unsafe addon paths, deterministic ordering, exact `/sale,/stock` construction after module sort/deduplication, byte-for-byte explicit changed `--tags` passthrough, and proof that no fetch/pull/network/mutation command runs.

## 3. Read-only Preflight and Single Runner

- [x] 3.1 Extend `internal/postgres_transport.run_psql()` with an optional target database while preserving existing default-`postgres` callers; add regression tests for exact argv/env/timeout/shell settings, socket/TCP/password-file behavior, and sanitized non-zero/missing/timeout outcomes.
- [x] 3.2 Implement one bounded read-only preflight for exactly one configured database and all selected `ir_module_module.state='installed'` rows; test missing/ambiguous databases, unavailable authentication/tooling, query failure, missing/uninstalled modules, escaped validated names, success, and proof that no write/install/update/restore or Odoo spawn occurs.
- [x] 3.3 Refactor `internal/automation.py` so one `run_odoo_tests(instance, spec, ...)` replaces the duplicate-prone legacy runner path, checks the bound port, sets process-local workers to zero, calls `_run_shell_script_exclusive()` and `odoo.tests.shell.run_tests()` exactly once, and keeps rollback-only execution under the existing environment lock.
- [x] 3.4 Add runner tests for exact module/tag/reload arguments, native `testsRun`/success/failure/error/skipped mapping, process failure, zero tests with and without `allow_empty`, port conflict, workers setting, one shell invocation, no regex log inference, and sanitized native diagnostics returned only for stderr emission.

## 4. CLI Adapter, Alias, and Output

- [x] 4.1 Add focused `commands/test.py`, register `odcli test [TARGET]`, and wire the MYL-55 typed context plus shared format options; validate `TARGET` singularity and every incompatible combination (`TARGET+--changed`, base/dry-run without changed, file+tags) as exit-2 usage errors before resolver/Git/preflight/Odoo calls.
- [x] 4.2 Implement module/cwd/file/changed orchestration in the adapter so only executable non-empty selections build one `OdooTestSpec`, using the deterministic changed default selector or byte-exact explicit tags; dry-run and `no_addon_changes` construct neither spec nor result and stop before preflight, unsafe mappings return collected provenance non-zero, and executable plans perform preflight then one runner call.
- [x] 4.3 Convert `odcli module test` into a parser-compatible alias that validates all legacy plural modules through the same selector, preflight, spec, runner, and result functions; remove the old independent behavior path and prove one-module equivalence plus sorted/deduplicated plural behavior.
- [x] 4.4 Project the exact executed/dry-run/no-addon state matrix into command-local Rich and envelope-v1 machine output: keep common environment/selection/modules/exit and base provenance, include tags/options/counts/flags only for executed results, and use `dry_run=true` or `reason="no_addon_changes"` for non-executed success; parameterize JSON/strict-TOON parity and assert absent execution-only fields, no fabricated counts/progress, alias format conflicts, one-document/no-ANSI stdout, sanitized stderr-only diagnostics, and renderer-independent execution/exit behavior.
- [x] 4.5 Add CLI tests for explicit root `--env`, exact worktree inference, ambiguous/outside context rejection, module/cwd/file examples, changed environment-base and explicit-base provenance, complete dry-run fields, docs-only no-op, fatal unmapped paths, and assertions that every pre-Odoo failure leaves the runner uncalled.

## 5. Integration, Documentation, and Delivery

- [x] 5.1 Add one `real_odoo` opt-in disposable integration test with a temporary installed addon that exercises selector → read-only preflight → `OdooTestSpec` → existing shell runner → `OdooTestResult`, including cleanup and no install/update/restore side effect during the command.
- [x] 5.2 Update user-facing CLI documentation with module, cwd, file, native class/method tags, changed/base/dry-run, docs-only behavior, direct-addons-only limitation, no automatic mutation, and `module test` compatibility examples.
- [x] 5.3 Run focused unit/integration suites, the disposable Odoo test when prerequisites are available (otherwise record the explicit prerequisite skip), full `make pr`, `git diff --check`, and `openspec validate add-context-aware-odoo-tests`; fix only regressions within this change.
- [x] 5.4 Per the approved delivery override, verify the selected remote's push URL is SSH (`git@github.com:maximchikAlexandr/odoo-instance-sdk.git` or equivalent), push `feat/MYL-56-changed-odoo-tests` without changing unrelated remotes, and record the final SHA; no PR is required, and the delivery excludes CI dispatch, dependency graphs, cache, coverage selection, sharding, or unrelated CLI moves.
