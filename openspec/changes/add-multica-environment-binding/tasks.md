# Tasks

## 1. Predecessor and external SDK gates

- [ ] 1.1 Confirm merged public source/COPY/readiness contracts from MYL-271/272 and workspace foundation from #69; update the exact-version table in `research.md` and verify named-source and retained-UUID COPY plus detached-readiness contract tests pass. Do not implement their missing features in this change.
- [ ] 1.2 In the separately maintained `multica-py`, deliver the narrow public native repository `checkout_command`/`checkout` with typed single-path output, existing transport/options/redaction and bounded timeout. Verify fake-executable cases for valid/malformed output, missing task context, stderr, timeout and cancellation; publish supported version evidence before consuming it here. No private daemon HTTP adapter in odcli-multica.
- [ ] 1.3 Add the narrowly required daemon/server/OS/status and workspace-runtime identity fields to the public `multica-py` daemon-status result/decoder using the existing v0.5.3 JSON contract. Consume `daemon.status_command`, `issues.get_command` and `issues.runs_command` for the exact host/run/path comparison. Contract-test missing evidence, starting/stopped/port-conflict, forwarded filesystem and paginated run membership as fail-closed; do not claim the integration release compatible until a valid active task passes the same checks.

## 2. Core caller-owned checkout adoption

- [ ] 2.1 Add one linear additive catalog migration plus public frozen environment evidence for code ownership, explicit artifact root, configured project identity and actual external Git identity. Verify old valid SDK layouts keep ownership, ambiguous rows stay unknown, fresh/upgraded schemas match and only one Alembic head remains; document forward-only migration.
- [ ] 2.2 Add `EnvironmentResource.adopt_command`/`adopt` using existing immutable planning and COPY provisioning after code acquisition. Parameterize linked-worktree/independent-clone success, source selectors, wrong repository/base/dirty input, secret isolation, venv explicitness and plan/execution parity; document exact first-adoption preconditions.
- [ ] 2.3 Reuse provisioning locks/catalog reservation for stable project+checkout identity and exact matching retries. Verify concurrent adoption creates at most one environment, lost-response retry returns the same UUID without download/restore, and incompatible or incomplete state yields conflict/recovery rather than overwriting data.
- [ ] 2.4 Remove assumptions that every environment's project is its Git common dir or that artifact root is code-path parent from the affected list/cwd/config/sync/runtime/doctor/monitor paths. Verify an independent external clone resolves by environment UUID and cwd, appears under the configured project and launches using rebased addon paths without changing the source template; document lifecycle usage.
- [ ] 2.5 Make rollback/removal ownership-aware before exposing adoption. Verify dirty/missing/replaced external checkout and parent remain untouched, SDK-owned COPY data/config can be safely removed by UUID, active Odoo/unknown evidence fail closed, and legacy owned checkout cleanup still works. Include symlink/root-containment and partial-cleanup retries.

## 3. Extension installation and context

- [ ] 3.1 Add only `packages/odcli-multica` to the #69 workspace, with independent metadata/executable, supported dependency ranges and package-qualified release rules. Verify a no-sources build installs without undeclared cross-member or private SDK imports; document standalone and combined tool installation.
- [ ] 3.2 Add the concrete composition client, `ProjectLink` and `TaskContext`, atomic project link/show and bounded context SDK commands with CLI leaves. Test changed-file rejection, explicit issue/project/workspace/repository/runtime matching, ambiguity, pagination and owner-only secret-free persistence; document setup for a third project without copying a full passport.
- [ ] 3.3 Reuse or publish the narrow public core `cli_output` facade over existing bounded document emission and frozen output types. Test JSON/TOON/Rich parity and missing-package behavior in installed wheels; keep callbacks as SDK delegates and the existing core leaf inventory authoritative. Document only the stable extension-facing API.

## 4. Prepare and binding operations

- [ ] 4.1 Implement extension `prepare_command`/`prepare` and `env prepare`: validate task context then return/run the captured core adoption command, COPY only with explicit source/base. Verify wrong-host/context fails before adoption, success never binds/starts Odoo, and prepare retry returns the same environment; document native checkout as a separate preceding phase.
- [ ] 4.2 Implement `bind_command`/`bind` and `unbind_command`/`unbind` with one ignored owner-only local file per environment and extension-local locks. Parameterize identical retry, same-issue next-run association, conflicting issue, concurrent writes, project-link drift, bind-vs-core-remove race and offline unbind; prove no remote task/resource mutation or environment deletion. Document local-only association semantics.
- [ ] 4.3 Implement bounded `status_command`/`status` and `env status` from public core/Multica facts. Test bound/unbound/stale/conflict/unavailable, missing/replaced code, normal code edits, removed core record and provisioned-but-stopped state with retained identities and actionable next operations; document Multica code-GC lifetime limitations.
- [ ] 4.4 Add parameterized SDK/CLI preview/error/interruption/secret-projection matrices for every new leaf, including task-token and source-password diagnostics. Verify no mutation during planning, command siblings delegate once, stdout is one machine document, and no complete-phase retry is suggested after partial failure; document a short multi-phase Python composition and equivalent CLI recipe.

## 5. Integrated acceptance and publication

- [ ] 5.1 Run deterministic temporary-repository/fake-Multica integration: native result → adopted COPY → bind → explicit core start/readiness → stop → unbind/remove. Assert exactly one code checkout, one target database, isolated filestore, stable UUID retry and no borrowed-code deletion. Include two tasks in one Multica project without project resource rewrites.
- [ ] 5.2 Run the same flow in a separately approved disposable Multica daemon workspace with a compatible Odoo test fixture; prove actual run cwd/checkout identity, SDK database isolation, preserved external code and typed partial recovery. Retain sanitized exact-version evidence; do not substitute static source inspection for this release gate or upload real project data.
- [ ] 5.3 Run core-only wheel, extension-only tool and combined-tool smoke jobs, core+extension compatibility and available all-extension smoke, per-distribution Ruff/strict mypy, schema/architecture/command parity contracts, `git diff --check` and strict OpenSpec validation. Publish only the changed distribution(s) with recorded compatibility; no implementation task is complete merely because this planning package validates.
